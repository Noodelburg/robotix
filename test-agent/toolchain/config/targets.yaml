# targets.yaml
# ------------
# Example approved targets for the scaffold. These are placeholders and should be
# replaced with real, explicitly approved non-production targets in actual use.

targets:
  - name: staging-api
    environment: staging
    base_url: https://api.staging.example.com
    protocols:
      - https
    active_testing_allowed: true

  - name: staging-web
    environment: staging
    base_url: https://staging.example.com
    protocols:
      - https
    active_testing_allowed: true
