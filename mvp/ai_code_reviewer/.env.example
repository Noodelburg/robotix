# Start with the mock provider so you can run the project without an API key.
REVIEWER_PROVIDER=mock

# Switch this to openai when you want real model-backed reviews.
OPENAI_API_KEY=your_api_key_here

# A smaller model keeps the MVP cheaper and faster.
OPENAI_MODEL=gpt-5-mini

